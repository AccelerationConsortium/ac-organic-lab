"use client";

import { PlatesPanel } from "./PlatesPanel";

/** /utils/plates — where every plate is, and the bench-top move front door. */
export default function PlatesPage() {
  return <PlatesPanel />;
}
